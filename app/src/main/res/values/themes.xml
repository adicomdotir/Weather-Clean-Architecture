<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.WeatherCleanArchitecture" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="Theme.WeatherCleanArchitecture.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/sunny</item>
        <item name="windowSplashScreenAnimationDuration">200</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_rweather_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.WeatherCleanArchitecture.NoActionBar</item>
    </style>

    <style name="Theme.WeatherCleanArchitecture.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.WeatherCleanArchitecture.bg">
        <item name="android:background">@color/sunny</item>
    </style>

    <style name="Theme.RWeather.CustomCardview" parent="CardView">
        <item name="cardBackgroundColor">@color/cardSunny</item>
    </style>
</resources>